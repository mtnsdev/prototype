/**
 * When false, Demo badges are hidden (e.g. when real backend is connected).
 */
export const IS_PREVIEW_MODE = true;

/**
 * Briefing Room: when no user in session, treat as agency admin (Kristin) so admin widgets show.
 * Set to false to see advisor-only UI without logging in. With a real user, role still wins.
 */
export const BRIEFING_PREVIEW_DEFAULT_ADMIN = true;
